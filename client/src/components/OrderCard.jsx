import { useRef } from 'react';

const OrderCard = (props) => {
  console.log(props)

  return (
    // <div ref={innerRef}>
    //   {order._id}
    // </div>
    <></>
  )
  };

export default OrderCard;
